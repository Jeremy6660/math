# 论文与代码待修改问题汇总

> 本文档汇总当前论文 `paper_outline.tex` 与代码 `solution.py` 中尚未解决的所有问题，按优先级与问题维度分类。已完成项见历史会话记录。

---

## 一、问题1：关键指标筛选与体质贡献度（剩余问题）

### 1.1 逐步Logistic回归：注释与实现不一致
- **位置**：`solution.py` 第69行，`stepwise_logit` 函数注释
- **问题**：注释写"基于AIC的前向+后向逐步Logistic回归"，但实际代码逻辑完全基于 `p-value` 阈值（`threshold_in=0.05`，`threshold_out=0.10`），未使用AIC。论文中已改为如实描述，但代码注释仍未修正。
- **修改方案**：将函数注释改为"基于p-value阈值的前向+后向逐步Logistic回归"。

### 1.2 逐步Logistic回归未处理完全分离（Complete Separation）
- **位置**：`solution.py` 第72--76行
- **问题**：当某个候选变量与因变量高度相关时（如TG与高血脂），`sm.Logit().fit()` 可能出现完全分离，导致系数估计发散、标准误无穷大。代码中仅用 `try/except` 捕获异常并将p值设为1.0，但没有记录或提示用户。
- **修改方案**：在 `except` 分支中打印警告信息；或改用 `Logit.fit_regularized()` 进行Firth惩罚似然估计。

### 1.3 体质贡献度分析中缺少VIF的实际计算
- **位置**：`solution.py` 第148--163行
- **问题**：论文表~\ref{tab:const} 中列出了VIF值（如气虚质VIF=4.2），但代码中**完全没有计算VIF**。这些数值是编造的。
- **修改方案**：在代码中增加VIF计算并输出，确保论文中的VIF数据有代码支撑。
  ```python
  from statsmodels.stats.outliers_influence import variance_inflation_factor
  vif_data = pd.DataFrame({
      '体质': constitution_cols,
      'VIF': [variance_inflation_factor(X_const.values, i)
              for i in range(X_const.shape[1])]
  })
  ```

### 1.4 缺少非线性交互效应探索
- **位置**：`solution.py` 问题1模块
- **问题**：题目背景提到"脾失健运、痰湿阻滞恰与西医血脂代谢异常的病理机制高度契合"，提示可能存在交互效应（如痰湿积分 x 气虚积分）。当前模型仅假设线性可加关系，未探索交互项。
- **修改方案**：增加痰湿积分与血脂核心指标（TG、TC）的交互项分析，或绘制散点图矩阵观察非线性模式。

---

## 二、问题2：三级风险预警模型（剩余问题）

### 2.1 风险评分权重完全依赖主观设定
- **位置**：`solution.py` 第208行，`paper_outline.tex` 第250--258行
- **问题**：综合风险评分 Ri = 0.5*Pi + 0.3*(S_phlegm/100) + 0.2*(N_abn/4) 的权重 (0.5, 0.3, 0.2) 是人为指定的，无任何客观计算依据。论文中提到熵权法但并未实际实施。
- **修改方案**：
  1. **熵权法**：计算 Pi、S_phlegm/100、N_abn/4 三个指标的信息熵，客观赋权。
  2. **敏感性分析**：将主观权重扰动 +/-20%，观察三级分层比例的变化，证明模型对权重选择不敏感。

### 2.2 三级风险分层缺乏独立的统计验证
- **位置**：`solution.py` 问题2模块，`paper_outline.tex` 第354--356行
- **问题**：论文声称"LDA模型的三级分类总体准确率为78.3%，高风险召回率为82.4%"，但代码中**完全没有计算这些指标**。这些数字是编造的。此外，用LDA去验证Logistic回归的分层结果存在逻辑循环（二者使用相同特征）。
- **修改方案**：
  1. 在代码中实现K折交叉验证（如5折分层交叉验证），在训练集上拟合Logistic回归并确定阈值，在测试集上评估分层准确率、召回率、F1。
  2. 绘制ROC曲线，计算AUC，量化模型区分高风险与非高风险的能力。
  3. 引入独立的验证方法（如决策树或随机森林）进行模型对比，而非LDA。

### 2.3 缺少风险分层的可视化
- **位置**：`solution.py` 问题2模块
- **问题**：当前代码仅输出阈值和频数分布，无任何可视化（如风险评分直方图、ROC曲线、高风险人群特征雷达图等）。
- **修改方案**：增加matplotlib/seaborn可视化：
  - 三级风险评分的分布直方图（带阈值竖线）
  - ROC曲线（高风险 vs 非高风险）
  - 核心特征组合的韦恩图或Upset图

---

## 三、问题3：干预方案优化模型（剩余问题）

### 3.1 单目标优化过于简化，违背老年医学常识
- **位置**：`solution.py` 第239--284行，`paper_outline.tex` 第436--457行
- **问题**：模型将目标函数设为"最小化6个月后痰湿积分"，成本仅作为硬约束（Cost <= 2000元）。这导致**所有患者的最优频率全部被推到 f=10次/周**。对于中老年人群（尤其是80--89岁或活动能力低下者），每周10次干预在生理耐受度、依从性和安全性上极不现实。
- **修改方案**：
  1. **双目标优化框架**：将问题重新定义为"最小化痰湿积分"与"最小化总成本"的帕累托（Pareto）双目标优化，绘制帕累托前沿曲线，让患者/医生根据偏好选择方案。
  2. **依从性衰减函数**：高龄患者（年龄组5）或活动能力低下者（M<40）若频率过高，设置依从性惩罚。例如：当 f>5 且年龄组=5 时，实际有效频率 = f * 0.8。
  3. **干预疲劳效应**：每周10次对老年人而言几乎不可持续，模型应引入频率上限的软性约束（如建议 f <= 7次/周为"可持续区间"）。

### 3.2 穷举法虽正确但数学深度不足
- **位置**：`solution.py` 第258--284行，`paper_outline.tex` 第458--477行
- **问题**：决策空间仅30种组合，穷举法在工程上正确，但竞赛论文中若仅写穷举法，数学建模深度不够，容易被评委认为"缺乏建模含金量"。
- **修改方案**：
  1. **解析解推导**：证明在成本约束内，目标函数关于 y 和 f 单调递减。据此推导闭合形式的最优解公式，穷举法仅作为数值验证。
  2. **公式示例**：
     - 若 6*C_T(x) + 24*10*C_A(y_max) <= 2000，则 y*=y_max, f*=10；
     - 否则 f* = floor((2000 - 6*C_T(x)) / (24*C_A(y)))，取使 r 最大的 y。

### 3.3 缺少成本-效果的敏感性分析
- **位置**：`solution.py` 问题3模块，`paper_outline.tex` 第526--534行
- **问题**：论文中给出"单位成本降分效果"的具体数字（如0.0167分/元），但这些数值是硬编码的，代码中没有计算过程，且未考虑参数扰动。
- **修改方案**：
  1. 在代码中自动化计算单位成本降分效果。
  2. 对干预效果参数（基础下降率3%、额外下降率1%）进行敏感性分析：若实际效果波动 +/-20%，最优方案是否稳定？

---

## 四、论文撰写与学术规范（剩余问题）

### 4.1 参考文献严重不足
- **位置**：`paper_outline.tex` 第568--575行
- **问题**：仅3篇参考文献，其中《数模》那篇与主题无关。数学建模竞赛论文通常需要8--12篇。
- **建议补充方向**：
  - 王琦. 中医体质学（原著）
  - 中医体质与血脂异常关联的流行病学研究
  - Framingham风险评分改良版在老年人群中的应用
  - 多目标优化在医疗资源配置中的文献
  - 中国成人血脂异常防治指南（2023年更新版）

### 4.2 缺少模型评价与改进的可操作性
- **位置**：`paper_outline.tex` 第537--559行
- **问题**：模型缺点和改进方向写得过于笼统（"引入PCA、GAM、随机扰动项"），缺乏与本文具体模型的结合。
- **修改方案**：
  1. 针对本文的具体参数和结构提出改进，如"本文风险评分权重采用专家经验法，改进方向为引入CRITIC熵权法客观赋权"。
  2. 针对问题3的干预效果公式提出改进："当前下降率为确定性线性公式，改进方向为引入Logistic饱和曲线，反映运动干预的边际递减效应"。

### 4.3 摘要与结论的老年群体特色不够突出
- **位置**：`paper_outline.tex` 第49--55行，全文各处
- **问题**：当前论文偏重方法论，缺乏对老年人群特殊性的深入讨论。
- **建议补充**：
  - **年龄分层差异**：不同年龄段的风险阈值是否应差异化设置？（如70岁以上患者即使血脂轻度异常也应提高警惕）
  - **ADL/IADL的医学意义**：不仅是预测变量，更是老年功能评估（Geriatric Assessment）的核心指标，与跌倒风险、失能风险直接相关。
  - **"治未病"思想的落地**：模型如何体现"未病先防、既病防变、瘥后防复"三级预防？当前模型主要覆盖了前两级的风险评估，缺少"瘥后防复"的随访机制设计。

### 4.4 论文中存在未在代码中实现的统计方法
- **位置**：`paper_outline.tex` 第180--187行（已删除），但全文可能还有其他类似问题
- **问题**：论文不应声称代码中未实现的方法。
- **排查清单**：
  - [x] Bootstrap稳健性检验（已删除）
  - [x] VIF值（论文有数据，代码未计算）
  - [x] LDA交叉验证准确率（论文有数据，代码未计算）
  - [ ] 问题3中"单位成本降分效果"的具体数字（论文有，代码未计算）

---

## 五、代码工程化与可复现性（剩余问题）

### 5.1 缺少可视化输出
- **位置**：`solution.py` 全文
- **问题**：当前代码纯文本输出，无任何图表保存。竞赛论文需要配图支撑。
- **修改方案**：增加matplotlib绘图并保存为PNG/PDF：
  - 问题1：各指标与痰湿质/高血脂的相关性热力图
  - 问题2：风险评分分布直方图 + ROC曲线
  - 问题3：帕累托前沿图（若实施双目标优化）、不同活动方案的积分下降曲线

### 5.2 缺少随机种子设置
- **位置**：`solution.py` 第1--10行
- **问题**：若后续引入Bootstrap或交叉验证，sklearn的部分算法（如LogisticRegression的求解器）可能因随机性产生微小差异。
- **修改方案**：在文件开头增加 `np.random.seed(42)`。

### 5.3 硬编码的样本ID输出不够灵活
- **位置**：`solution.py` 第292--301行
- **问题**：样本ID 1、2、3的输出是硬编码循环，未考虑这些样本可能不是痰湿体质（constitution!=5）的情况。
- **修改方案**：增加断言或条件判断，确保输出的3个样本确实是痰湿体质患者；若不是，应提示并自动从痰湿质患者中选取前3例。

---

## 六、优先级排序建议

| 优先级 | 修改项 | 影响 |
|:---:|---|---|
| **P0** | 修复表3、表4的LaTeX编译错误 | **已完成** |
| **P0** | 补充问题1痰湿体质严重程度分析 | **已完成** |
| **P0** | 修正代码语法错误与数据路径 | **已完成** |
| **P1** | 在代码中实际计算VIF、LDA准确率、单位成本效果 | 论文数据造假风险 |
| **P1** | 问题3改为双目标优化或增加依从性约束 | 模型合理性 |
| **P1** | 增加K折交叉验证与ROC/AUC | 方法严谨性 |
| **P1** | 补充8--12篇参考文献 | 学术规范 |
| **P2** | 熵权法客观赋权 + 敏感性分析 | 方法深度 |
| **P2** | 推导问题3解析解替代纯穷举 | 数学深度 |
| **P2** | 增加可视化图表 | 论文表现力 |
| **P3** | 增加交互项分析、非线性探索 | 理论完整性 |
