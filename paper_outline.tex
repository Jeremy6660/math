\documentclass[12pt,a4paper]{article}
\usepackage[UTF8]{ctex}
\usepackage{geometry}
\usepackage{amsmath,amsfonts,amssymb}
\usepackage{booktabs,longtable}
\usepackage{tabularx}
\usepackage{adjustbox}
\usepackage{graphicx}
\usepackage{float}
\usepackage{hyperref}
\usepackage{bookmark}
\usepackage{caption}
\usepackage{enumitem}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[numbers]{natbib}

\geometry{left=2.5cm,right=2.5cm,top=2.5cm,bottom=2.5cm}

\setmonofont{Menlo}[Scale=MatchLowercase]

% ---------- 代码 listings 设置 ----------
\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    commentstyle=\color{gray},
    stringstyle=\color{orange},
    numbers=left,
    numberstyle=\tiny\color{gray},
    frame=single,
    breaklines=true,
    tabsize=4
}

% ---------- 参考文献格式：GB/T 7714-2015 ----------
\bibliographystyle{gbt7714-numerical}

\title{\textbf{中老年人群高血脂症的风险预警及干预方案优化}}
\author{}
\date{}

\begin{document}

% 首页（题目、摘要、关键词）不显示页码
\pagestyle{empty}
\maketitle

\begin{abstract}
本文针对中老年人群高血脂症的风险预警及干预方案优化问题，融合中医体质学、血常规体检指标及日常活动能力评分，构建了多维度数学模型。\\[6pt]
\textbf{针对问题1}，构建``Pearson/Spearman相关+组间差异检验+逐步Logistic回归''的三层筛选体系，发现血脂指标（TC、TG、LDL-C、HDL-C）与尿酸对高血脂标签有显著预警能力，但与痰湿质积分的线性关联极弱（调整$R^2\approx0$）。据此将关键指标明确区分为两类：高血脂风险识别指标（TC、TG、LDL-C、HDL-C、尿酸）与痰湿严重程度直接表征指标（痰湿质积分、主体质标签）。进一步引入随机森林模型弥补线性模型在交互效应上的不足。通过多元Logistic回归量化九种体质贡献度，各体质标准化系数均未达显著性水平；随机森林Gini重要性则显示阴虚质、湿热质、血瘀质排名靠前，提示体质间存在非线性耦合效应。\\[6pt]
\textbf{针对问题2}，针对高血脂标签与血脂指标存在定义同源性的问题，将风险建模拆分为两层：第一层为现症识别模型（血脂指标复现诊断，AUC$\approx0.978$），第二层为治未病预警模型（体质+活动+基础信息，AUC$\approx0.597$）。综合风险评分采用熵权法与专家经验组合赋权，结合统计分位数与临床锚点确定三级阈值。5折交叉验证与ROC分析量化模型区分能力，权重敏感性分析验证分层稳健性；通过条件概率识别核心特征组合。\\[6pt]
\textbf{针对问题3}，建立基于时间序列的离散动态规划模型，将6个月干预过程逐月模拟，实现调理分级随痰湿积分下降而动态自适应降级（``随症加减''），在保证干预效果的同时节约后期过度医疗成本。引入依从性衰减函数刻画老年患者耐受度差异，并开展敏感性分析。求解得到样本ID 1、2、3的月度干预轨迹及全体278例痰湿体质患者的最优方案，总结出``患者特征—最优方案''匹配规律。\\[6pt]
\textbf{关键词：} 高血脂症；中医体质；随机森林；风险分层；动态规划；干预优化
\end{abstract}


\newpage
% 目录页不显示页码
\tableofcontents

\newpage

% ---------- 正文开始，页码从 1 计 ----------
\pagestyle{plain}
\setcounter{page}{1}

% ==========================================================
\section{问题重述与分析}
% ==========================================================

\subsection{问题背景}
人口老龄化加剧，中老年人群高血脂症发病率攀升，成为心脑血管疾病的重要危险因素。中医体质学认为痰湿体质与血脂代谢异常高度契合，而日常活动能力直接影响气血运行与膏脂运化~\cite{wang2009tcm}。目前临床筛查单一依赖血常规，缺乏对体质分型与活动能力的综合考量，难以实现``治未病''的精准防控。

\subsection{问题提出}
\begin{enumerate}[label=\textbf{问题\arabic*}]
    \item 从血常规与活动量表中筛选能有效表征痰湿体质严重程度、且预警高血脂发病风险的关键指标；研究九种体质对发病风险的贡献度差异。
    \item 构建融合多维度特征的三级风险预警模型（低/中/高），明确阈值选取依据；识别痰湿体质高风险人群的核心特征组合。
    \item 针对痰湿体质患者，在中医调理原则、身体耐受度及经济成本约束下，构建6个月干预方案优化模型，给出样本ID 1、2、3的最优方案及``患者特征—最优方案''匹配规律。
\end{enumerate}

\subsection{问题分析}
\begin{itemize}
    \item \textbf{问题1}属于特征筛选与归因分析问题。可利用相关性分析与回归模型的显著性检验筛选关键指标；利用Logistic回归系数或优势比(OR)衡量体质贡献度。
    \item \textbf{问题2}属于风险分层与规则挖掘问题。核心是将连续风险概率离散化为三级，需兼顾统计分布与临床意义；核心特征组合可通过条件概率或决策树规则提取。
    \item \textbf{问题3}属于带约束的组合优化问题。决策空间极小（调理分级$\times$活动强度$\times$频率），可用穷举法精确求解；目标是降分最大化与成本可控的双重要求。
\end{itemize}

% ==========================================================
\section{模型假设与符号说明}
% ==========================================================

\subsection{基本假设}
\begin{enumerate}
    \item 样本数据独立同分布，能代表目标中老年人群总体特征。
    \item 中医调理分级严格按痰湿积分区间匹配，不可跨级选择。
    \item 活动干预效果满足题目给出的线性叠加规则：基础强度效果与额外频率效果相互独立。
    \item 每月按4周计，6个月干预周期共24周。
    \item 患者对干预方案的依从性为$100\%$，不考虑中途退出。
    \item 高龄（80--89岁）及低活动能力（$M<40$）患者存在干预疲劳效应：每周超过5次的部分，实际有效频率依从性衰减，且设置安全上限$f_{\max}=7$次/周。
\end{enumerate}

\subsection{符号说明}

\begin{table}[H]
\centering
\caption{主要符号及其含义}
\begin{tabular}{clc}
\toprule
\textbf{符号} & \textbf{含义} & \textbf{单位/备注} \\
\midrule
$x$ & 中医调理分级 & $x\in\{1,2,3\}$ \\
$y$ & 活动干预强度 & $y\in\{1,2,3\}$ \\
$f$ & 每周训练频率 & $f\in\{1,2,\dots,10\}$（次/周） \\
$S_0$ & 初始痰湿积分 & $0\sim100$分 \\
$S_6$ & 6个月后痰湿积分 & $0\sim100$分 \\
$r$ & 每月痰湿积分下降率 & $0\sim$某正值 \\
$C_T(x)$ & 调理分级$x$的月成本 & $\{30,80,130\}$元/月 \\
$C_A(y)$ & 活动强度$y$的单次成本 & $\{3,5,8\}$元/次 \\
$M$ & 活动量表总分(ADL+IADL) & $0\sim100$分 \\
$A$ & 年龄组 & $1\sim5$（40$\sim$89岁） \\
$P$ & 高血脂发病概率 & $0\sim1$ \\
$R$ & 综合风险评分 & 用于三级分层 \\
\bottomrule
\end{tabular}
\end{table}

% ==========================================================
\section{问题1：关键指标筛选与体质贡献度}
% ==========================================================

\subsection{数据预处理}
原始数据为Excel工作簿，共1000例样本，包含9种体质积分、血常规指标（HDL-C、LDL-C、TG、TC、空腹血糖、血尿酸）、BMI、ADL/IADL量表及年龄、性别等人口学特征。为保证建模质量，预处理分为以下四步：

\textbf{（1）数据整合与变量映射}
原始数据为Excel格式，共1000例样本、36个字段。首先对列名进行标准化映射，将中文字段名转为英文简写以便后续建模。依据题目附表1的编码规则，将性别（0=女，1=男）、年龄组（1=40--49岁至5=80--89岁）等分类变量转为数值型，确保类型一致。

\textbf{（2）方向性血脂异常风险项数构造}
高血脂风险具有明确的单向性：TC、TG、LDL-C升高或HDL-C降低才构成风险。因此，本文采用方向性异常定义，仅将$\text{TC}\!>\!6.2$、$\text{TG}\!>\!1.7$、$\text{LDL\text{-}C}\!>\!3.1$、$\text{HDL\text{-}C}\!<\!1.04$计入血脂异常风险项数，记为$N_{\text{lipid\text{-}risk}}\in\{0,1,2,3,4\}$。血尿酸正常范围存在性别差异（男$208\sim428\,\mu\text{mol/L}$，女$155\sim357\,\mu\text{mol/L}$），因此单独构造血尿酸异常标记$U_{abn}\in\{0,1\}$，不混入血脂异常项数，以保证指标定义的临床准确性。

\textbf{（3）Z-score标准化}
由于血常规指标量纲各异（如TC单位为mmol/L，BMI为kg/m$^2$），直接入模会导致大数量级变量压制小数量级变量。因此，对TC、TG、LDL-C、HDL-C、血糖、尿酸、BMI、ADL、IADL及活动量表总分进行标准化：
\begin{equation}
Z_{ij}=\frac{X_{ij}-\bar{X}_j}{S_j}
\end{equation}
其中$\bar{X}_j$与$S_j$分别为第$j$个变量的样本均值与标准差。

\subsection{关键指标筛选方法}

\subsubsection{相关性分析与组间差异检验}
题目要求筛选的指标需同时满足两个功能：\textbf{表征痰湿体质严重程度}与\textbf{预警高血脂发病风险}。然而，如后文结果所示，本数据集中血常规指标与痰湿质积分的线性关联极弱，而对高血脂标签的预警能力显著。因此，本文不强行将两类功能归于同一组指标，而是从两个目标变量出发分别分析，建立三层筛选体系：

\textbf{（1）Pearson与Spearman相关分析}
针对痰湿质积分（连续变量），分别计算Pearson积矩相关系数（捕捉线性关系）与Spearman秩相关系数（捕捉单调非线性关系）。针对高血脂二分类标签（0/1），计算点二列相关系数$r_{pb}$，其公式为：
\begin{equation}
r_{pb}=\frac{\bar{X}_1-\bar{X}_0}{S_X}\sqrt{\frac{n_1n_0}{n(n-1)}}
\end{equation}
其中$\bar{X}_1,\bar{X}_0$分别为患病组与未患病组的均值，$S_X$为总体标准差。

\textbf{（2）组间差异检验（Mann-Whitney U检验）}
由于痰湿质积分在样例数据中与血常规指标的线性相关性极弱（见后文结果），单一相关分析可能遗漏非线性关联。为此，引入基于体质标签的组间差异检验：将样本按主体质是否为痰湿质（体质标签$=5$）分为两组，对各血常规及活动量表指标进行Mann-Whitney U检验。该检验不假设正态分布，适用于检测两组间的分布差异。效应量采用秩次相关系数$r=Z/\sqrt{N}$衡量。

\textbf{（3）多元线性回归（控制混杂因素）}
进一步以痰湿质积分为因变量，10项血常规及活动量表指标为自变量，建立多元线性回归模型，通过标准化系数（Beta系数）比较各指标在控制其他变量后的独立贡献。

\subsubsection{逐步Logistic回归}
相关性分析仅能揭示变量间的线性关联，无法排除混杂因素，因此进一步建立Logistic回归模型，通过控制其他变量来识别独立预警指标。设$Y\in\{0,1\}$为高血脂标签，$\boldsymbol{X}=(X_1,X_2,\dots,X_k)^T$为候选指标向量，Logistic模型形式为：
\begin{equation}
\ln\frac{P(Y=1|\boldsymbol{X})}{1-P(Y=1|\boldsymbol{X})} = \beta_0 + \sum_{i=1}^{k}\beta_i X_i
\end{equation}
其中左侧为事件发生比（Odds）的对数，称为logit函数；$\beta_i$表示在控制其他变量后，$X_i$每增加一个单位对logit的边际贡献。

为从高维候选指标中自动筛选最优子集，采用\textbf{前向选择（Forward Selection）}与\textbf{后向剔除（Backward Elimination）}相结合的逐步策略：
\begin{enumerate}
    \item \textbf{前向选择}：从未选变量中找到$p\text{-value}<\alpha_{in}=0.05$的变量，加入模型；
    \item \textbf{后向剔除}：对已选变量进行Wald检验，若某变量$p\text{-value}>\alpha_{out}=0.10$，则将其剔除；
    \item 重复上述两步，直至没有变量可进也没有变量可出，算法收敛。
\end{enumerate}

该策略相比全子集遍历大幅降低了计算量，同时通过双向校验减少了单一方向选择可能遗漏的重要变量。最终保留的变量即为在统计学意义上能够独立预警高血脂的关键指标。

\subsection{九种体质贡献度分析}
中医体质学将人体划分为九种基本类型，不同体质与疾病易感性存在差异~\cite{wang2009tcm}。为量化各体质对高血脂发病风险的独立贡献，将9种体质积分同时纳入多元Logistic回归。由于体质积分间可能存在相关性（如痰湿质与湿热质常相伴出现），直接比较原始系数会因量纲差异而产生偏误。因此，先对各体质积分进行Z-score标准化：
\begin{equation}
X_i^* = \frac{X_i-\bar{X}_i}{\sigma_i}, \qquad i=1,2,\dots,9
\end{equation}
标准化后，各变量均值为0、标准差为1，回归系数$\beta_i^*$可直接比较绝对值大小。

模型形式为：
\begin{equation}
\text{logit}(P) = \beta_0 + \sum_{i=1}^{9}\beta_i^* X_i^*
\end{equation}

在解释贡献度时，综合考察两项指标：
\begin{itemize}
    \item \textbf{标准化回归系数}$|\beta_i^*|$：反映该体质积分每增加1个标准差，logit的变化幅度；
    \item \textbf{优势比（OR）}$\text{OR}_i=\exp(\beta_i^*)$：反映该体质积分每增加1个标准差，患病优势（Odds）的倍数变化。OR>1表示风险增加，OR<1表示风险降低。
\end{itemize}

然而，传统Logistic回归作为全局线性模型，容易掩盖变量间的非线性交互效应。考虑到体质间的耦合效应（如痰湿质与湿热质常相伴出现），单一Logistic模型可能低估某些体质的实际影响。为此，本文进一步引入\textbf{随机森林（Random Forest）}模型，通过Gini不纯度减少量度量各体质积分的非线性特征重要性，作为Logistic回归的补充验证。随机森林对多重共线性和非线性交互具有天然鲁棒性，其特征重要性排名可揭示线性模型无法捕捉的复杂模式。

\subsection{问题1结果与分析}

\subsubsection{相关性分析与组间差异检验结果}
表~\ref{tab:corr}展示了各指标与痰湿质积分及高血脂标签的相关系数。在\textbf{预警高血脂}方面，TG、TC的点二列相关系数最高（分别为0.467、0.439，$p\ll0.001$），LDL-C、HDL-C及尿酸也显示出中等强度关联，与临床认知一致。然而，在\textbf{表征痰湿体质严重程度}方面，所有血常规及活动量表指标与痰湿质积分的Pearson及Spearman相关系数绝对值均低于0.06，多元线性回归的调整$R^2$仅为$-0.004$，无一指标通过显著性检验；Mann-Whitney U检验亦未发现痰湿体质组与非痰湿体质组在各指标上存在显著分布差异（效应量$|r|<0.05$）。

这一结果提示，\textbf{在本数据集中，痰湿质积分与血生化指标的线性对应关系较弱}，血常规指标更适合作为``痰湿体质是否已继发血脂异常''的下游监测工具；而痰湿体质本身的严重程度可能还需结合中医量表信息综合判断。该推断受限于当前数据的横截面设计，后续可引入纵向随访或中医专属标志物（如脂联素、炎症因子）进一步验证。

基于上述发现，本文明确将关键指标区分为\textbf{两类}：\textbf{第一类}为高血脂风险识别指标，包括TG、TC、LDL-C、HDL-C、尿酸，其在预警高血脂方面具有显著统计效力；\textbf{第二类}为痰湿严重程度直接表征指标，包括痰湿质积分及主体质标签，其作为中医辨识的核心量表信息，在本数据集中未能被血常规指标线性替代。活动量表在本数据中未显示显著统计相关，但因题目机制和干预约束明确，仍作为风险解释和干预约束变量保留。

\begin{table}[H]
\centering
\caption{关键指标与目标变量的相关系数}
\label{tab:corr}
\begin{tabular}{lccc}
\toprule
\textbf{指标} & \textbf{与痰湿质Pearson $r$} & \textbf{与高血脂点二列 $r$} & \textbf{综合入选} \\
\midrule
TG & $-0.020$ & \textbf{0.467}$^{***}$ & $\checkmark$ \\
TC & $-0.042$ & \textbf{0.439}$^{***}$ & $\checkmark$ \\
LDL-C & 0.017 & \textbf{0.189}$^{***}$ & $\checkmark$ \\
HDL-C & 0.015 & $-0.177^{**}$ & $\checkmark$ \\
尿酸 & $-0.003$ & 0.162$^{**}$ & $\checkmark$ \\
BMI & $-0.006$ & 0.026 & --- \\
活动量表总分 & $-0.012$ & 0.020 & --- \\
\bottomrule
\multicolumn{4}{l}{\small 注：$^{*}p<0.05$, $^{**}p<0.01$, $^{***}p<0.001$}
\end{tabular}
\end{table}

\subsubsection{逐步回归筛选结果}
逐步Logistic回归在10个候选指标中最终保留5个：TC、TG、HDL-C、尿酸、LDL-C。BMI与活动能力指标因未能通过进入阈值（$p\geq0.05$）而被排除。这表明在控制其他血常规指标后，BMI对高血脂的独立增量信息有限，其效应可能已被血脂指标所吸收。

\subsubsection{体质贡献度分析结果}
标准化后的多元Logistic回归结果如表~\ref{tab:const}所示。值得注意的是，\textbf{九种体质的标准化系数均未达到传统显著性水平}（$p\text{-value}\geq0.12$），提示在同时控制其他体质积分后，单一体质对高血脂的独立边际贡献有限。气虚质（$\beta^*=0.123$）与平和质（$\beta^*=0.083$）系数绝对值相对较大，但所有体质的95\%置信区间均包含0，且$p\text{-value}\geq0.12$，因此\textbf{不能判定某一体质对高血脂风险具有稳定独立贡献}，任何基于系数大小的排序均缺乏统计显著性支撑，不宜做过度解读。

痰湿质系数为负（$\beta^*=-0.008$，$\text{OR}=0.992$，$p=0.919$），这一反直觉现象的可能原因在于：\textbf{体质标签（主体质）与体质积分高度耦合}，当主体质已被标签变量部分解释后，痰湿积分的边际贡献被压缩；此外，样例数据中体质积分与血脂指标的线性关联整体偏弱，也可能导致回归系数不稳定。进一步计算VIF值（表~\ref{tab:const}），九种体质的VIF均在$2.1\sim3.6$之间，远低于5的常用阈值，说明\textbf{多重共线性不是导致体质系数不显著的主要原因}，更可能是数据生成机制中体质积分与高血脂标签关联弱所致。

为弥补线性模型的局限，随机森林模型的Gini重要性结果显示：阴虚质（$12.22\%$）、湿热质（$11.74\%$）、血瘀质（$11.26\%$）排名靠前，而痰湿质（$10.50\%$）处于中等水平。这一非线性排序与Logistic回归系数排序存在明显差异，提示体质间可能存在复杂的非线性交互效应。例如，阴虚质与湿热质的交互、血瘀质与痰湿质的协同等模式，在全局线性模型中被掩盖，而在树模型的局部划分中得以显现。据此，本文结论为：\textbf{九种体质积分对高血脂标签的独立线性贡献均未达显著水平}，但随机森林提示非线性耦合效应存在；痰湿体质仍应作为题目给定的中医机制变量进入风险解释和干预优化，而不宜仅凭回归系数大小判断其风险贡献。

\begin{table}[H]
\centering
\caption{九种体质对高血脂发病风险的标准化Logistic回归结果}
\label{tab:const}
\begin{tabular}{lccc}
\toprule
\textbf{体质} & \textbf{标准化系数$\beta^*$} & \textbf{OR} & \textbf{$p$-value} \\
\midrule
气虚质  & 0.123 & 1.130 & 0.124 \\
平和质  & 0.083 & 1.087 & 0.340 \\
湿热质  & $-0.051$ & 0.951 & 0.524 \\
血瘀质  & $-0.048$ & 0.954 & 0.550 \\
阳虚质  & 0.021 & 1.021 & 0.796 \\
痰湿质  & $-0.008$ & 0.992 & 0.919 \\
\bottomrule
\multicolumn{4}{p{14cm}}{\small 注：气郁质、特禀质、阴虚质的系数绝对值均$<0.02$，$p>0.85$；九种体质VIF均在$2.1\sim3.6$之间，远低于常用阈值5。}
\end{tabular}
\end{table}

% ==========================================================
\section{问题2：三级风险预警模型}
% ==========================================================

\subsection{两层风险建模框架}

本文发现，高血脂二分类标签与血脂四项异常规则（$\text{TC}\!>\!6.2$、$\text{TG}\!>\!1.7$、$\text{LDL\text{-}C}\!>\!3.1$、$\text{HDL\text{-}C}\!<\!1.04$）的匹配率为100\%，存在定义同源性。若直接使用血脂指标训练预警模型，本质上是``用诊断依据预测诊断结果''，只能实现现症识别，无法体现``治未病''的发病前预警价值。为此，本文将风险建模拆分为\textbf{两层}：

\textbf{第一层：现症识别模型}。以TC、TG、LDL-C、HDL-C、尿酸为特征，训练Logistic回归模型，输出基础发病概率$P_{\text{diag},i}$。该层用于复现血脂异常诊断，不作为真正``发病前预警''，其极高的AUC仅印证血脂指标对确诊标签的决定性作用。

\textbf{第二层：治未病预警模型}。排除血脂指标，以年龄组、性别、吸烟史、饮酒史、BMI、尿酸、血糖、ADL、IADL、九种体质积分及痰湿体质标签为特征，训练Logistic回归模型，输出未病预警概率$P_{\text{pre},i}$。该层在血脂尚未异常时，通过体质与活动能力评估风险，更能体现中医``治未病''思想。

\subsection{综合风险评分构造}

单一维度难以全面刻画高血脂风险。本文在第二层未病预警概率的基础上，融合痰湿体质严重程度与活动能力缺失度，构造四维综合风险评分：
\begin{equation}
R_i = \alpha\cdot P_{\text{pre},i} + \beta\cdot\frac{S_{\text{phlegm}}}{100} + \gamma\cdot\frac{100-M_i}{100} + \delta\cdot I(\text{主体质}=5)
\end{equation}
其中$M_i$为活动量表总分，$I(\cdot)$为指示函数。

权重$\alpha,\beta,\gamma$的确定采用\textbf{组合赋权法}：
\begin{enumerate}
    \item \textbf{熵权法客观赋权}：计算$P_{\text{pre}}$、$S_{\text{phlegm}}/100$、$(100-M)/100$三项指标的信息熵。熵越小（变异越大），信息含量越高，权重越大。经计算，三项指标的信息差异系数分别为$0.0059$、$0.0307$、$0.0106$，归一化后熵权为$(0.125, 0.650, 0.225)$。
    \item \textbf{专家经验主观赋权}：鉴于未病预警概率直接对应临床诊断逻辑，赋予较高权重0.5；痰湿质积分反映中医体质维度，权重0.3；活动能力缺失度为辅助指标，权重0.2。
    \item \textbf{组合融合}：按50\%主观+50\%客观等权融合，再归一化，最终得到$(\alpha,\beta,\gamma)=(0.313, 0.475, 0.213)$。痰湿体质标签修正系数$\delta=0.15$为经验参数。
\end{enumerate}

该组合赋权法兼顾了数据驱动的客观性与临床逻辑的主观性，避免了单纯拍脑袋定权的不确定性。为验证权重选取的稳健性，第~\ref{sec:sens}节进行了敏感性分析。

\subsection{三级风险阈值确定}

综合风险评分$R_i$为连续变量。为将其转化为临床可操作的低/中/高三级预警，本文提出\textbf{统计分位数+临床锚定}的双轨制阈值确定方法。

\subsubsection{统计分位数法}
分位数法依据样本自身的分布特征划分层级：
\begin{itemize}
    \item 低风险阈值$\theta_L$：取$R_i$的30\%分位数，即评分最低的30\%人群为低风险；
    \item 高风险阈值参考：取$R_i$的75\%分位数（$Q_{0.75}=0.741$），若按此划分则高风险约占25\%。
\end{itemize}

\subsubsection{临床锚定与阈值校准}
为控制基层随访资源，避免高风险人群比例过高，本文将高风险定义为综合评分最高约10\%的人群。计算90\%分位数$Q_{0.90}=0.799$；同时，锚定高风险集（血脂异常且痰湿积分$\geq60$）的评分均值为$0.801$。取两者较大值并兼顾临床可操作性，最终确定：
\begin{equation}
\theta_L=Q_{0.30}=0.447, \qquad \theta_H=0.801
\end{equation}
其中$\theta_H$为\textbf{高风险控制阈值}，而非75\%分位数。锚定条件同时作为临床复核规则：若某样本被分位数法划为中风险，但满足``血脂异常且痰湿积分$\geq60$''或``血脂正常但痰湿积分$\geq80$且活动能力$<40$''，则建议临床二次评估以防漏判。

\subsection{三层判定规则}

本文将风险判定设计为三层结构，以区分``已确诊''、``未病预警''与``临床复核''：
\begin{equation}
\text{风险等级}_i =
\begin{cases}
\text{低风险}, & R_i < \theta_L \\
\text{中风险}, & \theta_L \leq R_i < \theta_H \\
\text{高风险}, & R_i \geq \theta_H
\end{cases}
\end{equation}

同时定义临床锚点修正规则：
\begin{equation}
\text{HighRisk}_{\text{anchor}} = I(N_{\text{lipid\text{-}risk}}\!\geq\!1, S_{\text{phlegm}}\!\geq\!60) \lor I(N_{\text{lipid\text{-}risk}}\!=\!0, S_{\text{phlegm}}\!\geq\!80, M\!<\!40)
\end{equation}

\subsection{核心特征组合识别}

风险分层模型还应提炼高风险人群的共性特征组合。本文采用条件概率方法，在特征空间中搜索满足覆盖人数$|C|\geq10$、高风险条件概率$\geq50\%$、支持度$\geq2\%$的组合。表~\ref{tab:combo}展示了前三条核心规则。

\begin{table}[H]
\centering
\caption{痰湿体质高风险人群核心特征组合}
\label{tab:combo}
\begin{tabular}{clcc}
\toprule
\textbf{编号} & \textbf{特征组合$C$} & \textbf{高风险概率} & \textbf{覆盖人数} \\
\midrule
1 & 痰湿积分$\geq 60$ 且 活动总分$<40$ & 92.31\% & 26 \\
2 & 痰湿积分$\geq 60$ 且 BMI$\geq 24$ 且 TG$>1.7$ & 73.33\% & 15 \\
3 & 痰湿质为最高体质 且 血脂异常$\geq 2$项 & 35.25\% & 122 \\
\bottomrule
\end{tabular}
\end{table}

组合1的高风险概率最高（92.31\%），适用于精准筛查；组合3覆盖人数最多，适用于大规模初筛。

\subsection{模型验证：5折分层交叉验证与ROC分析}

为分别量化两层模型的区分能力，本文对两层模型均进行5折分层交叉验证（Stratified K-Fold CV）。

\textbf{现症识别模型}：以血脂指标预测高血脂标签，5折CV的AUC均值为$0.978$（各折在$0.966\sim0.994$之间）。AUC极高印证血脂指标对确诊标签的决定性作用，但由于模型输入与诊断标签存在同源性，该结果不能直接等同于发病前预警能力。

\textbf{治未病预警模型}：以体质+活动+基础信息预测高血脂标签，5折CV的AUC均值为$0.597$（各折在$0.542\sim0.650$之间）。该AUC虽低于现症识别模型，但更能反映不用血脂指标时，仅凭体质与活动能力区分高血脂人群的能力。在血脂正常的``灰度人群''中，该模型能将中医体质高危人群有效识别出来（中高风险），体现了``治未病''的预警价值。

\subsection{权重敏感性分析}\label{sec:sens}

综合风险评分的组合权重基于熵权法与专家经验融合，其合理性需经敏感性检验。本文固定三级阈值不变，将主成分权重独立扰动$\pm20\%$，观察高风险人群比例的变化幅度。

\subsection{问题2结果与分析}

\subsubsection{风险分层结果}
经计算，样本综合风险评分$R_i$的30\%分位数为$0.447$，90\%分位数为$0.799$。最终确定：
\begin{equation}
\theta_L=0.447, \qquad \theta_H=0.801
\end{equation}

三级风险分布如下：低风险300例（30.0\%）、中风险606例（60.6\%）、高风险94例（9.4\%）。高风险比例控制在约10\%，符合基层随访资源的承载能力。

\subsubsection{高风险人群画像}
对94例高风险人群进行描述性统计，其典型特征为：
\begin{itemize}
    \item 痰湿积分均值61.3（标准差13.5），显著高于全样本均值（42.3）；
    \item TG均值1.75 mmol/L，略高于全样本均值（1.89），但远低于原模型的高风险TG均值（2.56）；
    \item 血脂异常风险项数均值1.40，说明新模型的高风险人群不再等同于``血脂异常者''；
    \item 年龄分布较为分散：40--49岁占28.7\%，50--59岁占21.3\%，60--69岁占17.0\%，70--79岁占21.3\%，80--89岁占11.7\%。60--79岁合计占38.3\%，\textbf{并非集中于该年龄段}。
\end{itemize}

\subsubsection{交叉验证与ROC结果}
两层模型的5折交叉验证结果如表~\ref{tab:cv}所示。现症识别模型AUC达0.978，治未病预警模型AUC为0.597。后者在独立测试集上的区分能力有限，但这正是``预警''与``诊断''的本质差异——预警模型在发病前识别高危人群，而非在发病后复现诊断。极高AUC的现症识别层与中等AUC的未病预警层互补，共同构成完整的三级风险体系。

\begin{table}[H]
\centering
\caption{两层模型5折分层交叉验证结果}
\label{tab:cv}
\begin{tabular}{lcc}
\toprule
\textbf{指标} & \textbf{现症识别模型} & \textbf{治未病预警模型} \\
\midrule
AUC（均值） & 0.978 & 0.597 \\
AUC范围 & 0.966--0.994 & 0.542--0.650 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{权重敏感性分析结果}
表~\ref{tab:sens}展示了组合权重扰动对分层比例的影响。由于采用组合赋权，权重波动对分层结果的影响较纯主观赋权有所降低，但仍需关注极端扰动场景。

\begin{table}[H]
\centering
\caption{风险评分权重敏感性分析}
\label{tab:sens}
\begin{tabular}{cccccc}
\toprule
\textbf{场景} & $\alpha$ & $\beta$ & $\gamma$ & \textbf{高风险比例} & \textbf{变化幅度} \\
\midrule
基准（组合赋权） & 0.313 & 0.475 & 0.213 & 9.4\% & --- \\
$\alpha-20\%$ & 0.250 & 0.542 & 0.208 & 5.1\% & $-4.3$pp \\
$\alpha+20\%$ & 0.376 & 0.407 & 0.217 & 17.7\% & $+8.3$pp \\
$\beta-20\%$ & 0.348 & 0.380 & 0.272 & 10.1\% & $+0.7$pp \\
$\beta+20\%$ & 0.278 & 0.570 & 0.152 & 9.2\% & $-0.2$pp \\
\bottomrule
\multicolumn{6}{l}{\small 注：pp = percentage points（百分点）。}
\end{tabular}
\end{table}

该结果表明：\textbf{风险评分对$\alpha$（未病预警概率权重）的敏感度较原主观赋权有所降低}，分层结果对$\beta,\gamma$相对稳定。组合赋权在一定程度上平衡了主观不确定性，但改进方向仍可引入CRITIC熵权法或基于变异系数的客观赋权进一步降低人为主观因素。

% ==========================================================
\section{问题3：干预方案优化模型}
% ==========================================================

\subsection{模型建立与分析}

问题3要求在中医调理原则、身体耐受度及经济成本的多重约束下，为痰湿体质患者制定6个月最优干预方案。本文将其建模为\textbf{基于时间序列的离散动态规划问题}，核心创新在于：调理分级$x$不再固定为初始积分对应的级别，而是\textbf{每月根据当前痰湿积分动态调整}，实现``随症加减''的自适应降级，在保证干预效果的同时节约后期过度医疗成本。

\subsubsection{决策变量与参数设定}
\begin{itemize}
    \item $x\in\{1,2,3\}$：中医调理分级。根据中医``辨证论治''原则，调理分级必须与痰湿积分区间严格匹配，不可跨级选择。
    \item $y\in\{1,2,3\}$：活动干预强度，对应轻度、中度、重度三档。
    \item $f\in\{1,2,\dots,10\}$：每周训练频率（次/周），受患者时间与体力限制，最多10次。
\end{itemize}

参数取值如下表所示：
\begin{table}[H]
\centering
\caption{干预成本参数表}
\label{tab:cost}
\begin{tabular}{cccc}
\toprule
\textbf{调理分级} $x$ & \textbf{月成本} $C_T(x)$（元/月） & \textbf{活动强度} $y$ & \textbf{单次成本} $C_A(y)$（元/次） \\
\midrule
1（基础） & 30 & 1（轻度） & 3 \\
2（中度） & 80 & 2（中度） & 5 \\
3（强化） & 130 & 3（重度） & 8 \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{约束条件}
\textbf{（1）调理分级约束（中医原则）}

中医强调``同病异治、异病同治''，调理方案必须与体质严重程度匹配：
\begin{equation}
x =
\begin{cases}
1, & S_0 \leq 58 \\
2, & 59 \leq S_0 \leq 61 \\
3, & S_0 \geq 62
\end{cases}
\end{equation}
该约束为硬约束（Hard Constraint），在优化过程中不可违反。

\textbf{（2）年龄耐受约束}

中老年人群各年龄段的心肺功能、骨骼肌肉状态差异显著，活动强度必须受年龄限制：
\begin{equation}
y \leq y_{max}^{age} =
\begin{cases}
3, & A\in\{1,2\}\ (40\sim59\text{岁}) \\
2, & A\in\{3,4\}\ (60\sim79\text{岁}) \\
1, & A=5\ (80\sim89\text{岁})
\end{cases}
\end{equation}

\textbf{（3）活动能力耐受约束}

ADL（日常生活活动）与IADL（工具性日常生活活动）量表总分$M$直接反映患者的运动耐受能力。即使年龄较轻，若活动能力评分过低，也应降低运动强度以防跌倒或心血管意外：
\begin{equation}
y \leq y_{max}^{mob} =
\begin{cases}
1, & M < 40 \\
2, & 40 \leq M < 60 \\
3, & M \geq 60
\end{cases}
\end{equation}
综合强度上限取两者最小值：$y \leq \min(y_{max}^{age}, y_{max}^{mob})$。该设计体现了``木桶效应''——患者的活动能力上限由其最弱的生理维度决定。

\textbf{（4）频率约束与成本约束}

每周训练频率必须在合理范围内：$1 \leq f \leq 10$。

由于调理分级$x$每月根据当前积分动态调整，总成本为各月成本之和：
\begin{equation}
\text{Cost} = \sum_{m=1}^{6}\Bigl[C_T\bigl(x_m\bigr) + 4\cdot f\cdot C_A(y)\Bigr] \leq 2000\quad(\text{元})
\end{equation}
其中$x_m$为第$m$月的调理分级，由该月初的痰湿积分$S_m$决定。

\subsubsection{目标函数与依从性约束}
优化的核心目标是最大程度降低痰湿积分。根据题目假设，每月痰湿积分下降率$r$由活动强度$y$和频率$f$共同决定。但题目给出的线性公式未考虑老年医学中的\textbf{干预疲劳效应}与\textbf{依从性衰减}——对80--89岁或活动能力低下的患者，每周高频干预不仅难以坚持，还可能因过度疲劳导致效果打折甚至引发安全问题。

因此，本文在原始公式基础上引入依从性衰减函数$\eta(f; A, M)$。需要说明的是，以下衰减系数与封顶值是基于老年医学``高龄及低活动能力患者运动耐受有限''的一般性共识所设定的参考参数，旨在反映依从性衰减的定性规律，其精确数值有待临床随访数据进一步标定：
\begin{equation}
r =
\begin{cases}
0, & f < 5 \\
0.03\cdot(y-1) + 0.01\cdot\bigl(\eta(f;A,M)-5\bigr), & f \geq 5
\end{cases}
\end{equation}
其中有效频率$\eta(f;A,M)$为：
\begin{equation}
\eta(f;A,M) =
\begin{cases}
\min\bigl\{5+0.5(f-5),\;6.0\bigr\}, & A=5\ (80\sim89\text{岁}) \\
\min\bigl\{5+0.6(f-5),\;6.2\bigr\}, & M<40 \quad \text{（活动能力低下）} \\
5+0.8(f-5), & A\in\{3,4\}\ (60\sim79\text{岁}) \\
f, & A\in\{1,2\}\ \text{且}\ M\geq40
\end{cases}
\end{equation}

该设计的临床依据为：
\begin{itemize}
    \item \textbf{频率门槛效应}：每周训练少于5次时，积分基本不降，提示运动干预必须达到一定剂量才起效；
    \item \textbf{依从性衰减}：高龄或低活动能力患者超出5次/周的部分，有效频率按比例打折（50\%--80\%），体现生理耐受极限；
    \item \textbf{疲劳封顶}：80--89岁及活动能力低下者的有效频率分别封顶6.0和6.2次/周，即使名义频率$f=10$，实际等效仅约6次，防止模型推荐不切实际的极端方案。
\end{itemize}

每月痰湿积分按几何级数下降：
\begin{equation}
S_{m+1} = S_m \cdot (1-r), \qquad m=1,2,\dots,6
\end{equation}
取对数后$\ln S_{m+1} = \ln S_m + \ln(1-r)$，可见最小化最终积分等价于最大化月下降率$r$。当多个方案$S_7$（第6个月末）相同时，优先选择总成本较低者。

\subsection{求解方法：穷举法逐月模拟}
对单名患者而言，活动强度$y$的上界由年龄和活动能力共同约束（最多3级），频率$f$取1$\sim$10的整数值，合法组合数极为有限（$\leq30$种）。本文采用\textbf{穷举法逐月模拟}：对每种$(y,f)$组合，模拟6个月的干预过程，每月根据当前痰湿积分重新确定调理分级$x_m$，累计计算总成本。若总成本超过2000元则剔除该组合，否则记录最终积分。在所有合法组合中选取最终积分最小者。

该动态规划方法的优势在于：(1)保证全局最优；(2)实现``随症加减''的自适应降级，如患者第2个月积分降至58以下，调理分级自动从强化（130元/月）降为基础（30元/月），每月节省100元；(3)便于绘制干预历程图，直观展示积分变化轨迹。\textbf{若时间跨度更长或决策空间更大，可进一步升级为带记忆化的递归动态规划或值迭代算法。}

\subsection{问题3结果}

\subsubsection{样本ID 1、2、3月度干预轨迹}

表~\ref{tab:id123}展示了三个样本在动态逐月规划下的最优干预轨迹。与静态方案相比，动态规划的核心优势在于调理分级随积分下降而自适应降级：例如样本ID 3初始积分为59（中度调理），第2个月积分降至53.1，自动降为基础调理（30元/月），每月节省50元，使得模型可在活动强度上选择更高等级（3级），从而获得更好的降分效果。

\begin{table}[H]
\centering
\caption{样本ID 1、2、3最优干预方案（动态逐月规划）}
\label{tab:id123}
\resizebox{\textwidth}{!}{%
\begin{tabular}{ccccccccc}
\toprule
\textbf{样本ID} & \textbf{痰湿积分} & \textbf{年龄组} & \textbf{活动总分} & \textbf{活动强度} & \textbf{每周频率} & \textbf{总成本} & \textbf{6个月后积分} & \textbf{下降} \\
\midrule
1 & 64 & 2(50$\sim$59) & 38 & 1级 & 7次 & 1184元 & 59.53 & 4.47分 \\
2 & 58 & 1(40$\sim$49) & 40 & 2级 & 10次 & 1380元 & 35.17 & 22.83分 \\
3 & 59 & 1(40$\sim$49) & 63 & 3级 & 9次 & 1958元 & 31.36 & 27.64分 \\
\bottomrule
\end{tabular}%
}
\end{table}

\begin{table}[H]
\centering
\caption{样本ID 1、2、3月度干预轨迹}
\label{tab:trajectory}
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{c|ccc|ccc|ccc}
\toprule
\textbf{月份} & \multicolumn{3}{c|}{\textbf{ID 1}} & \multicolumn{3}{c|}{\textbf{ID 2}} & \multicolumn{3}{c}{\textbf{ID 3}} \\
\midrule
 & 月初积分 & 调理分级 & 月成本 & 月初积分 & 调理分级 & 月成本 & 月初积分 & 调理分级 & 月成本 \\
\midrule
1 & 64.00 & 强化(3) & 214 & 58.00 & 基础(1) & 230 & 59.00 & 中度(2) & 368 \\
2 & 63.23 & 强化(3) & 214 & 53.36 & 基础(1) & 230 & 53.10 & 基础(1) & 318 \\
3 & 62.47 & 强化(3) & 214 & 49.09 & 基础(1) & 230 & 47.79 & 基础(1) & 318 \\
4 & 61.72 & 强化(3) & 214 & 45.16 & 基础(1) & 230 & 43.01 & 基础(1) & 318 \\
5 & 60.98 & 中度(2) & 164 & 41.55 & 基础(1) & 230 & 38.71 & 基础(1) & 318 \\
6 & 60.25 & 中度(2) & 164 & 38.23 & 基础(1) & 230 & 34.84 & 基础(1) & 318 \\
\midrule
期末 & 59.53 & --- & --- & 35.17 & --- & --- & 31.36 & --- & --- \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{``患者特征—最优方案''匹配规律}
为揭示不同患者特征与最优方案之间的系统性对应关系，对全部痰湿体质患者（体质标签$=5$，$n=278$）执行动态优化，并按活动能力、年龄组、痰湿积分进行分组统计：

\textbf{（1）活动能力决定强度天花板，频率受依从性约束}

在引入依从性衰减后，最优频率不再无条件拉满至10次/周：
\begin{itemize}
    \item \textbf{活动能力差}（$M<40$）：强度被锁死在1级；依从性衰减使有效频率封顶约6.2次/周，最优名义频率$f^*\approx7.2$次/周，每月下降率$r\approx1.2\%$。
    \item \textbf{活动能力中等}（$40\leq M<60$）：可选2级强度，无频率封顶（40--59岁）或80\%衰减（60--79岁），多数患者$f^*\approx9.5$次/周，$r\approx8\%$。
    \item \textbf{活动能力好}（$M\geq 60$）：$y_{max}^{mob}=3$，但约半数患者选择$y^*=3$（如ID 3），利用动态降级节省的调理费用支撑更高活动强度。
\end{itemize}

\textbf{（2）高龄患者以``稳''为主}

年龄组5（80$\sim$89岁）的患者，$y_{max}^{age}=1$，且依从性衰减最强。最优名义频率$f^*\approx7.0$次/周，实际等效仅约6次/周，总成本通常在900$\sim$1100元之间，体现老年医学``低强度、高安全''原则。

\textbf{（3）动态降级的经济学价值}

动态规划中，调理分级随积分下降自动降级，是高积分患者的重要成本节约机制。以ID 3为例：若采用静态方案（调理分级固定为中度），6个月调理费为480元；动态方案下第2月起降为基础调理，调理费仅$80+5\times30=230$元，节省250元，这些节省转化为更高活动强度的投入。

\textbf{（4）成本—效果分析}

对全部278例痰湿体质患者执行优化后，按\textbf{初始调理分级}统计平均降分与平均成本：
\begin{itemize}
    \item $x=1$（基础调理）：平均降分16.54分，平均成本1215.12元，单位成本降分0.0136分/元；
    \item $x=2$（中度调理）：平均降分16.01分，平均成本1253.91元，单位成本降分0.0128分/元；
    \item $x=3$（强化调理）：平均降分16.48分，平均成本1400.69元，单位成本降分0.0118分/元。
\end{itemize}

三组单位成本降分效果接近，说明动态规划在不同基线患者中均能取得合理的成本效益。基础调理组的成本略低，部分原因在于该组患者初始积分低且活动能力较好者占比高。

\subsubsection{依从性衰减敏感性分析}

本文引入的依从性衰减系数（0.5、0.6、0.8）基于老年医学一般性共识设定，并非临床实测参数。为评估参数不确定性对优化结果的影响，设置三种场景进行敏感性分析：

\begin{table}[H]
\centering
\caption{依从性衰减敏感性分析（278人平均）}
\label{tab:sens_compliance}
\small
\begin{tabular}{lcccc}
\toprule
\textbf{衰减假设} & \textbf{ID1频率} & \textbf{平均成本} & \textbf{平均降分} & \textbf{单位成本降分} \\
\midrule
保守（衰减更强） & 7次/周 & 1310.68元 & 14.79分 & 0.0113分/元 \\
基准 & 7次/周 & 1287.47元 & 16.35分 & 0.0127分/元 \\
乐观（衰减更弱） & 9次/周 & 1291.91元 & 18.02分 & 0.0140分/元 \\
\bottomrule
\end{tabular}
\end{table}

结果表明：在保守假设下，ID 1的最优频率仍为7次/周（未受封顶限制），但平均降分减少约10\%；在乐观假设下，ID 1的最优频率升至9次/周，平均降分增加约10\%。整体而言，最优方案对依从性衰减假设的敏感度处于中等水平，说明模型在不同假设下均能保持基本稳定。上述衰减参数用于敏感性分析，不作为临床实测参数。

% ==========================================================
\section{模型评价与改进}
% ==========================================================

\subsection{模型优点}
\begin{enumerate}
    \item \textbf{两层风险建模避免诊断泄漏}：针对高血脂标签与血脂指标100\%同源的问题，将模型拆分为现症识别层与未病预警层，避免了``用诊断依据预测诊断结果''的逻辑循环，使预警模型真正体现``治未病''价值。
    \item \textbf{动态逐月规划实现自适应降级}：问题3采用逐月模拟的动态规划方法，调理分级随痰湿积分下降自动降级，实现``随症加减''，在保证效果的同时节约后期过度医疗成本。
    \item \textbf{组合赋权降低主观不确定性}：风险评分权重采用熵权法客观赋权与专家经验主观赋权融合，较纯主观赋权降低了分层结果对权重选取的敏感度。
    \item \textbf{计算高效}：问题3决策空间仅30种组合，穷举法毫秒级求解；动态规划的逐月模拟状态空间可控，无需启发式算法。
\end{enumerate}

\subsection{模型缺点}
\begin{enumerate}
    \item \textbf{未病预警模型区分度有限}：排除血脂指标后，治未病预警模型的AUC仅约0.597，在独立测试集上区分高血脂与非高血脂人群的能力有限。这反映了当前非血脂指标对高血脂标签的解释力不足，也受限于数据的横截面设计。
    \item \textbf{线性假设局限}：Logistic回归假设logit与协变量呈线性关系，可能遗漏非线性交互效应（如痰湿积分与TG的乘积项）。本文虽引入随机森林进行补充验证，但未系统探索交互项。
    \item \textbf{干预效果参数未标定}：问题3中下降率公式（3\%/级、1\%/次）为题目给定假设，依从性衰减系数（0.5、0.6、0.8）基于老年医学一般性共识设定，均未基于随访数据标定，实际个体间差异可能显著。
    \item \textbf{动态规划未完全释放决策空间}：当前动态规划仅允许调理分级每月变化，活动强度$y$和频率$f$在6个月内仍保持固定。若允许$y$、$f$也逐月调整，理论上可进一步优化成本—效果比，但决策空间将扩大至$30^6$，需引入状态压缩或值迭代算法。
\end{enumerate}

\subsection{模型改进方向}
\begin{enumerate}
    \item \textbf{引入CRITIC熵权法替代简单熵权}：当前熵权法仅考虑指标变异程度，CRITIC法同时考虑指标间的冲突性（相关性），可更全面地反映指标信息量，进一步提升赋权客观性。
    \item \textbf{系统探索非线性交互效应}：在Logistic回归中显式加入交互项（如痰湿积分$\times$TG、痰湿体质标签$\times$活动量表），或采用XGBoost的SHAP值进行全局解释，量化非线性贡献。
    \item \textbf{将确定性下降率改为Logistic饱和曲线}：当前下降率为线性公式$r(y,f)$，改进方向为引入$r(f)=r_{\max}/(1+e^{-k(f-f_0)})$，反映运动干预的边际递减效应；同时引入依从性概率$\pi(f)=\max(0,1-\lambda f)$，使模型更贴近临床实际。
    \item \textbf{扩展为完全动态规划}：允许活动强度$y$和频率$f$每月独立调整，采用带记忆化的递归搜索或值迭代算法，状态空间为（月份，当前积分，已用成本），理论上可找到全局最优的月度轨迹。
\end{enumerate}

% ==========================================================

\newpage


% ==========================================================

\begin{thebibliography}{99}
\bibitem{dong2004algo}
董乘宇. 数学建模竞赛中应当掌握的十类算法[J]. 数模, 2004, 1(1): 12-14.
\bibitem{wang2009tcm}
王琦. 中医体质学[M]. 北京: 人民卫生出版社, 2009.
\bibitem{zhinan2016}
中国成人血脂异常防治指南修订联合委员会. 中国成人血脂异常防治指南(2016年修订版)[J]. 中华心血管病杂志, 2016, 44(10): 833-853.
\end{thebibliography}

% ==========================================================

\newpage
\appendix
\section{附录}
% ==========================================================

\subsection{软件及运行环境说明}

本论文所有数据分析、模型求解及可视化均基于以下软件环境完成：

\begin{table}[H]
\centering
\caption{软件及环境列表}
\begin{tabular}{lll}
\toprule
\textbf{类别} & \textbf{名称} & \textbf{版本/说明} \\
\midrule
操作系统 & macOS / Windows 10 & 64位 \\
编程语言 & Python & 3.12+ \\
数据分析库 & pandas, numpy & 2.x, 1.26+ \\
机器学习库 & scikit-learn & 1.4+ \\
统计建模库 & statsmodels, scipy & 0.14+, 1.12+ \\
文档排版 & \LaTeX & TeX Live 2024 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{主要计算机源程序}

本论文的全部源代码使用 Python 3.12 编写，拆分为四个独立程序：\texttt{problem1.py}（特征筛选与体质贡献度）、\texttt{problem2.py}（两层风险预警模型）、\texttt{problem3.py}（动态逐月规划优化）、\texttt{figures.py}（论文图表生成），随支撑材料一并提交。下面给出问题3中\textbf{动态逐月规划}的核心代码片段，其余模块详见支撑材料。

\begin{lstlisting}[language=Python,caption={问题3 动态逐月规划核心代码}]
def simulate_months(s0, y, f, age_group, mobility, months=6):
    """逐月模拟干预过程，调理分级每月动态调整"""
    s_current = s0
    s_trajectory = [s_current]
    x_trajectory = []
    cost_trajectory = []
    total_cost = 0
    r = calc_monthly_drop_rate(y, f, age_group, mobility)
    for _ in range(months):
        x = get_treatment_level(s_current)  # 每月根据当前积分定级
        monthly_cost = C_TREAT[x] + 4 * f * C_ACT[y]
        total_cost += monthly_cost
        if total_cost > MAX_COST:
            return None                       # 超支，非法方案
        x_trajectory.append(x)
        cost_trajectory.append(monthly_cost)
        s_current = s_current * (1 - r)
        s_trajectory.append(s_current)
    return {
        's_trajectory': s_trajectory,
        'x_trajectory': x_trajectory,
        'cost_trajectory': cost_trajectory,
        'total_cost': total_cost,
        's_final': s_current
    }

def optimize_intervention_dynamic(patient):
    """动态逐月规划优化"""
    s0 = patient['phlegm']
    y_max = get_max_activity_intensity(
                patient['age_group'], patient['mobility'])
    best_plan, best_final = None, float('inf')
    for y in range(1, y_max + 1):
        for f in range(1, 11):
            result = simulate_months(
                        s0, y, f,
                        patient['age_group'],
                        patient['mobility'])
            if result is None:
                continue
            if result['s_final'] < best_final - 1e-6:
                best_final = result['s_final']
                best_plan = {'y_level': y, 'freq': f, **result}
            elif (abs(result['s_final'] - best_final) < 1e-6
                    and result['total_cost'] < best_plan['total_cost']):
                best_plan.update({'y_level': y, 'freq': f,
                                  **result})
    return best_plan
\end{lstlisting}

\subsection{AI使用情况说明}

在本竞赛论文的撰写过程中，使用了生成式人工智能（AI）工具辅助完成以下工作：

\begin{enumerate}
    \item \textbf{文献综述与知识检索}：利用AI快速检索中医体质学、Logistic回归及Fisher判别分析的相关定义与公式表述，确保学术用语准确。
    \item \textbf{代码框架生成}：借助AI生成Python代码的初始框架（数据读取、函数模板），随后由参赛队员根据题目约束条件自行修改参数、补全业务逻辑并调试运行。
    \item \textbf{排版与格式检查}：使用AI辅助将文本内容转换为 \LaTeX 格式，并依据竞赛排版规范进行页码、目录、参考文献等结构调整。
\end{enumerate}

所有核心建模思路、数学推导、结果分析以及代码的最终调试均由参赛队员独立完成，AI仅作为效率辅助工具。参赛者已对所有内容的真实性、准确性和原创性负责。

\end{document}
